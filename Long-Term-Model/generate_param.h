double generate_param(int param_num){ // will add complexity to this to allow for different param drawing conditions (mean vs stochastic vs grid)

    double tmp; 

    double meanS = ; double sdS = ;
    double meanR = ; double sdR = ;
    double meanV = ; double sdV = ;
    double meanca = ; double sdca = ;
    double meanla = ; double sdla = ;
    
    if (param_num == 0) {tmp = }; //INIT S        // how do I get these to sometimes generate zeros? //just need for the initial generation. 
    if (param_num == 1) {tmp = }; //INIT R        // after populating with starting values, can later overwrite random ones with 0?
    if (param_num == 2) {tmp = }; //INIT V
    if (param_num == 3) {tmp = }; //c_a
    if (param_num == 4) {tmp = }; //l_a

    return tmp;
}